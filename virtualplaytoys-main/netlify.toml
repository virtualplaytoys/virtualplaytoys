[build]
  functions = "netlify/functions"
  publish = "."

# worker-secrets.json holds the workers' TOTP secrets and must never be
# downloadable from the site (only the serverless functions read it).
[[redirects]]
  from = "/worker-secrets.json"
  to = "/404"
  status = 404
  force = true
